flask-cors
Flask~=3.1.2
Flask-PyMongo
Flask-Admin
PyJWT~=2.10.1
werkzeug~=3.1.5
python-dotenv~=1.2.1
gunicorn
WTForms~=3.2.1
pymongo
email-validator
requests
python-telegram-bot
schedule
markdown
redis
cryptography
# Verifies the OIDC tokens Cloud Scheduler signs (internal/cron_routes.py).
# Pinned explicitly: it arrives transitively today, and a security boundary
# should not depend on another package's dependency graph.
google-auth
google-cloud-monitoring
google-cloud-bigquery
psycopg2-binary~=2.9.9
Flask-WTF>=1.1.1

# Tests only
pytest
mongomock
